events {}

http {
    upstream backend {
        server app1:5678;
        server app2:5678;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}